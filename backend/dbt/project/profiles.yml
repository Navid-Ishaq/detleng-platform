detleng_platform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'detleng') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'detleng_dev_password') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'detleng') }}"
      schema: analytics
      threads: 2
