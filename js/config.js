// Keep empty when the frontend and backend share one origin (recommended).
// If GitHub Pages is retained, set this to a separately hosted backend origin,
// for example: 'https://services.detleng.com'.
window.DETLENG_CONFIG = Object.freeze({
  backendOrigin: ''
});
